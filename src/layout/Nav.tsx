import { Box } from '@chakra-ui/react';

const Nav = () => {
  return (
    <Box
      style={{
        width: '100%',
        height: '100%',
        backgroundColor: '#333',
      }}
    >
      Nav
    </Box>
  );
};

export default Nav;
